const {
  DEV,
  APP_TYPES,
  POSTGRES_HOST,
  POSTGRES_USER,
  POSTGRES_PASSWORD,
  POSTGRES_DB,
  POSTGRES_PORT,
  FE_URL,
  API_DEFAULT_PAGE_LIMIT,
  API_URL,
  CRAWLER_URL,
  SECRET_KEY,
  SECRET_EXPIRATION,
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GOOGLE_ADMIN_EMAILS,
} = process.env;
export const Env = {
  DEV: DEV === 'true',
  APP_TYPES: APP_TYPES.split('|') as string[],
  POSTGRES_HOST,
  POSTGRES_USER,
  POSTGRES_PASSWORD,
  POSTGRES_DB,
  POSTGRES_PORT: parseInt(POSTGRES_PORT, 10),
  FE_URL,
  API_DEFAULT_PAGE_LIMIT: parseInt(API_DEFAULT_PAGE_LIMIT, 10),
  API_URL,
  CRAWLER_URL,
  SECRET_KEY,
  SECRET_EXPIRATION: parseInt(SECRET_EXPIRATION, 10),
  GOOGLE_CLIENT_ID,
  GOOGLE_CLIENT_SECRET,
  GOOGLE_ADMIN_EMAILS: GOOGLE_ADMIN_EMAILS.split('|'),
};
