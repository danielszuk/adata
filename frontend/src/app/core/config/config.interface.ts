// ***** README *****/
//  1. Change Here
//  2. Change assets / config / config.example.json
//  3. Change assets / config / config.json
export interface IConfig {
  backendUrl: string;
  inputTimeoutBeforeServerCall: number;
}
