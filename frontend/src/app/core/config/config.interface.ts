// ***** README *****/
//  1. Change Here
//  2. Change src / config / config.example.json
//  3. Change src / config / config.json
export interface IConfig {
  backendUrl: string;
  appUrl: string;
  inputTimeoutBeforeServerCall: number;
}
